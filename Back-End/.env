# Copy this file to .env and fill in real values.
# .env must NEVER be committed to git (it's already in .gitignore).

# ---- MongoDB ----
# Get this from MongoDB Atlas: Database > Connect > "Drivers" > copy the URI.
# Replace <password> with your real database user password.
MONGODB_URI=mongodb+srv://abhinavacharya707:Abhinav6360@cluster0.idckby7.mongodb.net/autotrack?appName=Cluster0

# ---- JWT ----
# Any long random string. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=c9c5a7bf48e719dea5c565ee7c77ba889afef012885382b758f2cfbe6445440adbae185f1754f27733dd2dfce9489303b461888bf5b565956c0bc4fe2559b6f1
JWT_EXPIRES_IN=7d

# ---- Server ----
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# ---- Gmail SMTP (for OTP emails) ----
# GMAIL_USER is the Gmail address that SENDS the OTP emails (not the user's email).
# GMAIL_APP_PASSWORD is a 16-character App Password from Google — NOT your normal
# Gmail password. See the setup instructions for how to generate one.
GMAIL_USER=abhinavacharya707@gmail.com
GMAIL_APP_PASSWORD=ponz fila wjbo cdhz

# ---- OTP settings ----
OTP_EXPIRES_MINUTES=10
